## Icon

```tsx
import React from 'react';
import { Icon } from 'rgulu-ui';
import './icon.demo.scss';
export default () => (
  <div>
    <Icon icon={'coffee'} size={'2x'} />
    <Icon icon={'coffee'} theme={'danger'} size={'2x'} />
    <Icon icon={'coffee'} theme={'main'} size={'2x'} />
    <Icon icon={'coffee'} theme={'success'} size={'2x'} />
    <Icon icon={'arrow-down'} theme={'success'} size={'2x'} />
    <Icon icon={'spinner'} size={'2x'} />
    <Icon icon={'square'} size={'2x'} />
    <Icon icon={'check'} size={'2x'} />
    <Icon icon={'address-card'} size={'2x'} />
    <Icon icon={'ad'} size={'2x'} />
    <Icon icon={'angellist'} size={'2x'} />
    <Icon icon={'angry'} size={'2x'} />
    <Icon icon={'arrow-circle-up'} size={'2x'} />
    <Icon icon={'apple-alt'} theme={'danger'} size={'2x'} />
    <Icon icon={'address-book'} size={'2x'} />
  </div>
);
```
